version: "3.9"
services:
  songmate-db:
    image: postgres
    restart: always
    ports:
    - 5432:5432
    environment:
      POSTGRES_PASSWORD: organizeit
      POSTGRES_USER: organizeit
      POSTGRES_DB: organizeit